services:
  tic-tac-toe:
    image: ghcr.io/jonathantsaitw/202510_lab1:main-5d864ee
    container_name: tic-tac-toe-game
    ports:
      - "9090:9090"
    restart: unless-stopped
    environment:
      - TZ=Asia/Taipei
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    labels:
      - "com.example.description=井字遊戲 - 靜態網頁應用"
      - "com.example.version=latest"